import { CustomButton2Directive } from './custom-button-2.directive';

describe('CustomButton2Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomButton2Directive();
    expect(directive).toBeTruthy();
  });
});
